# VegraAI — голосовой помощник в стиле Джарвиса

Голосовой ассистент на Python: голосовой ввод, ответ голосом, **нейросеть** для понимания запросов и управление ПК (приложения, поиск в браузере).

---

## Что нужно перед стартом

1. **Python 3.10+** (проверка: `python --version`)
2. **Микрофон**
3. **Интернет** — для распознавания речи (Google)
4. **Ollama** (для ответов нейросетью, не шаблонами) — [ollama.com](https://ollama.com), затем в терминале: `ollama pull llama3.2`

---

## Установка (по шагам)

### 1. Открой папку проекта в терминале

```powershell
cd c:\Users\asaas\Desktop\VegraAI
```

### 2. Установи все зависимости одной командой

```powershell
python -m pip install -r requirements.txt
```

Используй `python -m pip`, а не просто `pip`, чтобы пакеты встали в то окружение, откуда запускаешь `python main.py`.

*(По желанию: `python -m venv venv`, затем `venv\Scripts\Activate.ps1`, и уже в нём — `python -m pip install -r requirements.txt`.)*

### 3. Обучи нейросеть (один раз)

Нейросеть различает десятки сценариев: приложения, поиск, время, дата, привет/пока, а также **разговоры** — как дела, погода, еда, фильмы, музыка, игры, спорт, учёба, технологии, путешествия, здоровье, советы, поддержка, шутки и свободная болтовня.

```bash
python neural/train.py
```

Должны появиться файлы: `data/intent_model.pt` и `data/vocab.json`.

---

## Запуск

**С графическим интерфейсом (чат, переключатель Вкл/Выкл):**

```powershell
python gui_app.py
```

Или двойной щелчок по `run_gui.bat`. В окне: чат, поле ввода, кнопка **Отправить**, кнопка **🎤** (голос). Переключатель **«Голос: Вкл / Выкл»** — в положении **Вкл** доступны микрофон и озвучка ответов, в **Выкл** только текстовый режим.

**Только консоль (голос по кругу):**

```powershell
python main.py
```

Скажи в микрофон или напиши в чате, например:

**Команды:** *«Привет»*, *«Который час?»*, *«Открой блокнот»*, *«Найди в интернете рецепт борща»*, *«Пока»*.

**Просто поболтать:** *«Как дела»*, *«Расскажи анекдот»*, *«Что посмотреть»*, *«Устал от работы»*, *«Дай совет»*, *«Смысл жизни»*, *«Грустно»*, *«Пошути»*, *«Что поесть»*, *«Посоветуй игру»*, *«Поболтаем»* — и многое другое в `data/intents.json`.

---

## Настройка

### Приложения (`config.py`)

В `APPS` прописаны команды для фраз вроде «Открой X». Пример:

```python
APPS = {
    "блокнот": "notepad",
    "калькулятор": "calc",
    "проводник": "explorer",
    "браузер": "chrome",
    "хром": "chrome",
    # Chrome не открывается? Укажи полный путь:
    # "хром": r"C:\Program Files\Google\Chrome\Application\chrome.exe",
    ...
}
```

Добавляй свои приложения по тому же принципу.

### Поиск

По умолчанию — Google. Сменить можно в `config.py`:

```python
SEARCH_URL = "https://www.google.com/search?q={query}"
# или
SEARCH_URL = "https://yandex.ru/search/?text={query}"
```

### Голос (озвучка)

В `config.py`:

- `VOICE_INDEX` — 0 или 1 (другой голос в системе)
- `SPEECH_RATE` — скорость
- `VOLUME` — громкость 0.0–1.0

### Ответы нейросетью (Ollama)

На разговорные реплики (привет, как дела, советы, шутки и т.п.) VegraAI отвечает **LLM** (Ollama), а не шаблонами.

- Установи [Ollama](https://ollama.com), в терминале: `ollama pull llama3.2` (или `mistral`, `qwen2.5:3b`).
- В `config.py`: `LLM_ENABLED = True`, `LLM_MODEL = "llama3.2"` (подставь модель, которую скачал).
- Если Ollama не установлен или не запущен — будут использоваться шаблоны из `data/intents.json`.
- `LLM_ENABLED = False` — отключить LLM и всегда использовать шаблоны.

### Новые команды для нейросети

1. Открой `data/intents.json`.
2. В нужном `intent` добавь в `patterns` новые фразы-примеры.
3. При необходимости добавь новый `intent` с `tag`, `patterns` и `responses`.
4. Запусти заново обучение (обязательно после правок в `intents.json`):
   ```bash
   python neural/train.py
   ```

---

## Как это устроено

| Часть | Назначение |
|-------|------------|
| `voice_input.py` | Микрофон → текст (SpeechRecognition + Google) |
| `voice_output.py` | Текст → речь (pyttsx3) |
| `neural/intents_model.py` | Нейросеть (LSTM), определяет намерение по фразе |
| `neural/train.py` | Обучение нейросети по `data/intents.json` |
| `pc_controller.py` | Запуск приложений, открытие поиска в браузере |
| `assistant.py` | Связывает всё: голос → намерение → действие; для разговоров — LLM (Ollama) или шаблон |
| `main.py` | Цикл: слушать → обработать → сказать |
| `gui_app.py` | Окно с чатом, переключатель «Голос: Вкл/Выкл», кнопки Отправить и 🎤 |

---

## Частые проблемы

- **«Не расслышал»** — говори чётко, ближе к микрофону; проверь, что выбран нужный микрофон в системе.
- **`No module named 'speech_recognition'` / `pyttsx3` и т.п.** — поставь всё разом: `python -m pip install -r requirements.txt`.
- **«Модель не обучена»** — выполни `python neural/train.py`.
- **Chrome/Edge не открываются** — пропиши в `config.APPS` полный путь к `chrome.exe` / `msedge.exe`.
- **`weights_only` в `torch.load`** — если PyTorch старый и ругается, в `neural/intents_model.py` в `load_state_dict` замени  
  `torch.load(..., weights_only=True)` на `torch.load(...)`.
- **Ответы шаблонные, а не от нейросети** — проверь Ollama: установлен, `ollama pull llama3.2`, в конфиге `LLM_ENABLED = True` и `LLM_MODEL` совпадает с названием модели. Ollama должен быть запущен (часто висит в трее после установки).

---

## Дальнейшее развитие

- Локальное распознавание речи (Vosk/Whisper) — без интернета.
- Горячая клавиша или фраза-будильник, чтобы всегда слушать («Вебгра» / «Джарвис»).
- Управление громкостью, отключение экрана и т.п. через `pywin32`.

Удачи с твоим Джарвисом.
